# CitationRadar -- Architecture

## What this is
B2B alert/lead-gen tool built on public restaurant health-inspection-violation data for NYC (DOHMH) and Toronto (DineSafe). Buyers: pest control, commercial cleaning, refrigeration/HVAC companies, food-safety and restaurant consultants.

## Stack
- Next.js (App Router), Vercel Hobby, Neon Postgres (single free-tier project)
- Data ingestion: GitHub Actions ONLY, two independent workflows (one per city) -- never Vercel cron (Hobby cadence/duration limits), never server-side self-chaining, never browser-triggered fetching.
- All ingestion writes are BATCHED (chunks of ~500 rows per multi-row upsert), never one row per query.
- No local dev via GitHub-web-UI-only -- this project uses VS Code + git locally, Windows/PowerShell environment. Repo is public (deliberate choice).

## Data sources
- NYC: DOHMH Restaurant Inspection Results, Socrata (data.cityofnewyork.us), Local Law 11 of 2012 -- explicit "no restrictions," not subject to copyright.
- Toronto: DineSafe, CKAN-based (open.toronto.ca catalogue), Open Government Licence - Toronto -- explicit commercial-use grant. CONFIRMED (v1.7): open.toronto.ca/dataset/dinesafe/ is LIVE, daily-refreshed -- it renders via client-side JS, so static-HTML fetchers see a stale "Retired" banner -- ignore that. resource_id af0f5b8a-4b73-4a50-8781-65e949792b40, package_id b6b4f3fb-2e2c-47e7-931d-b87d22806948. Phone field confirmed native.

## City-scoping -- the core correctness property of this product
A NYC saved search must NEVER match a Toronto violation, and vice versa. Enforced at two independent layers:
1. Application logic: every matching query includes city_code as a hard condition (Session 10).
2. Database trigger: enforce_city_match() on search_matches raises an exception if a saved_search and violation have different city_code -- see schema.sql. A bug in the app layer cannot silently produce a cross-city alert; the database itself refuses the insert.

Session 10 includes a named, mandatory test proving this property holds.

## Monitoring -- two-class failure taxonomy
- LOUD failures (fetch throws, HTTP error, parse exception): GitHub Actions' own workflow-failure notification + a direct Resend email to the owner.
- SILENT failures (nothing looks broken, but something is wrong) -- checked at the end of every ingestion run, before marking status='success':
  a. Zero new rows when the dataset's own historical cadence says there should be some -- distinguish "not published yet" (log no_new_data) from "scraper reading stale/wrong data" (needs a human).
  b. Row count for this run is wildly different from that dataset's typical volume (order-of-magnitude check, not a fixed threshold).
  c. Expected fields missing from the API response (Socrata/CKAN both occasionally restructure columns) -- fail loudly, do not insert nulls.

Any of these sets ingestion_runs.status = 'partial', which triggers the same owner-facing alert email as a loud failure.

## Ingestion cadence vs. digest cadence
Ingestion (Sessions 4-5) runs DAILY, decoupled from the customer-facing digest, which stays WEEKLY (Session 11). Ingestion cost is trivial either way (Section 3), so run it daily and let violations sit in the database as soon as they're published -- this avoids stacking our own schedule on top of the source's own publish lag. Session 11's digest also gets a same-day CRITICAL-flag path, separate from the weekly batch: violations flagged CRITICAL by the source city go out as soon as the next daily ingestion run picks them up, not held for the weekly send. Everything else stays weekly.

## Free-tier constraints (verified)
- Vercel Hobby: cron cadence and duration limits are why ingestion runs on GitHub Actions instead -- see above. ToS prohibits commercial use; upgrade to Pro triggers on first real transaction or resource limits, whichever comes first -- not a fixed session number.
- Neon Free: single project sufficient (100 projects/account allowed on current free tier), 0.5GB storage cap per project -- monitor as both cities' violation tables grow.
- GitHub Actions: 2,000 free minutes/month on private OR public repos -- two city-scoped ingestion runs, once per day each, use a trivial fraction of this.

## Payments
Paddle as Merchant of Record. Built in Session 13, left dormant (no live checkout) until the owner is actually ready to charge real customers. Known Paddle account gotcha from a prior account: fill the Legal Name field and screenshot before submitting -- support response times can be slow.

## How to add corrections or new features in a later session
Never renumber or edit earlier sessions. Add a new session at the end of the current roadmap, state plainly which earlier session/file it corrects, and log it in the Corrections & Additions Log (Section 12). Same rule for schema.sql: append a dated migration block, never edit an existing table.

## Honesty commitments, implemented as real features (see Section 1.3)
- Contact info: NYC DOHMH's dataset natively includes a PHONE field (confirmed: 449,556 of 449,573 records populated, ~99.99% coverage) -- no external API, no cost, already covered by the same license as the rest of the dataset. Toronto's DineSafe schema is unconfirmed as of this writing -- Session 5 Step 0 checks for a native contact field before considering any external source. Do NOT default to a paid API (e.g. Google Places, which costs real money above a small free tier -- see Section 1.3 for the actual numbers) without exhausting free options first: the source dataset itself, then OpenStreetMap/Overpass (free, lower coverage) as a fallback only if Toronto's data has no phone field.
- Lag transparency: every ingestion run calculates median_lag_days (inspection_date to actual ingestion). Ingestion polls MORE frequently than each source's own update cadence, specifically so our pipeline never adds delay on top of the government's. The measured number becomes a marketing claim in Session 15, not a caveat.

## Two-city ceiling -- validation footprint, not the end state
NYC + Toronto's combined buyer pool across all five vendor categories is realistically in the hundreds, not thousands -- fine for proving the model, not the growth plan. city_code is a first-class field throughout the schema specifically so a third city is a new ingestion pipeline plus a cities row, not a redesign.

## Session log
(updated after every session)

- **Session 1** -- Project Setup. Created package.json (Next.js 14.x), app/layout.js, app/page.js (placeholder), vercel.json (empty crons array -- ingestion runs via GitHub Actions, not Vercel).
- **Session 2** -- Architecture doc + Design System. Created this ARCHITECTURE.md from the blueprint's Section 6. Added app/globals.css with a clean B2B compliance-tool design system, including a distinct visual tag/color per city (NYC vs Toronto) so the UI never lets a user forget which city's data they're looking at -- reinforces the Section 1 city-scoping requirement at the UI layer, ahead of the DB-level enforcement landing in Session 3/10.- **Session 3** -- Database Schema. Created db/schema.sql reproducing Section 5 exactly (22 statements: cities, establishments, violations, users, saved_searches, search_matches, ingestion_runs, prospects, plus the enforce_city_match() trigger). Ran it in Neon -- all 22 statements succeeded. Manually verified the trigger: a NYC saved search matched against a NYC violation succeeded; the same NYC search matched against a Toronto violation was correctly rejected with 'City mismatch: saved_search city=NYC, violation city=TOR. Cross-city match blocked.' Test data cleaned up afterward.
- **Session 4** -- NYC Ingestion (GitHub Actions, Batched, Enriched). Confirmed live against the DOHMH Socrata dataset (43nn-pn8j): record_date and phone fields are both live and populated as expected (Section 4). Created scripts/lib/monitor.js (shared silent-failure checks) and scripts/ingest-nyc.js -- fetches new/updated records incrementally via a watermark on inspection_date, normalizes violation category (pest/sanitation/temperature/other) via keyword matching, maps NYC's native phone field directly (no external API), chunks upserts into batches of 500, and writes an ingestion_runs row every time regardless of outcome. Added a migration (dated 2026-08-15): unique index on violations.source_row_hash to make re-runs idempotent. Created .github/workflows/ingest-nyc.yml -- runs daily at 10:00 UTC via cron, plus workflow_dispatch for manual runs, using DATABASE_URL and RESEND_API_KEY repo secrets (ALERT_EMAIL_TO/ALERT_EMAIL_FROM secrets not yet set -- alert emails are skipped gracefully until those exist). First measured median_lag_days: ~3.25 days (source's own publish lag, inspection_date to record_date) -- better than the ~5-day estimate in Section 4. Verified end-to-end: local run (first-run backfill, 25,081 rows, 90-day lookback), local re-run (1,421 rows, 144 new -- duplicates correctly skipped), and a GitHub Actions manual run (same window, 0 new -- correctly detected as already-ingested).
- **Session 5** -- Toronto Ingestion (GitHub Actions, Batched, Enriched). Confirmed live against the DineSafe CKAN dataset (package b6b4f3fb-2e2c-47e7-931d-b87d22806948, resource af0f5b8a-4b73-4a50-8781-65e949792b40): the dataset page is live behind a robots.txt block that only affects bot crawlers, not normal HTTP clients (confirmed by GitHub Actions and local runs working). Unlike NYC, Toronto's CSV has no incremental query API and no publish-timestamp field -- scripts/ingest-toronto.js downloads the full CSV each run and filters client-side against our own inspection_date watermark, computing median_lag_days as (ingestion time - inspection_date) rather than a source-provided field. Native phone field mapped directly, same as NYC, at zero cost. Toronto's inspectionStatus (pass/conditional/closed) mapped into violations.grade, matching the column comment set in Session 3. Created .github/workflows/ingest-toronto.yml -- daily cron at 10:30 UTC (offset 30 min after NYC), plus workflow_dispatch. Verified end-to-end: local run (first-run backfill, 11,577 rows), GitHub Actions manual run (749 rows in the 3-day overlap window, 0 new -- correctly deduped). First measured steady-state median_lag_days: ~4.2 days (the 46-day figure on the initial backfill run is expected and not representative -- it's averaged across the full 90-day lookback window, same pattern as NYC's first-run number). Confirmed no cross-city contamination: establishments and violations tables cleanly split by city_code (NYC: 6,068 establishments / TOR: 7,338 establishments after each city's 90-day backfill).
- **Session 6** -- Monitoring & Alert System. Upgraded scripts/lib/monitor.js from a flat-threshold heuristic (Session 4's stopgap) to the full design: check (a) zero new rows is only flagged if overdue past the city's known ~24h publish cadence (1.5x slack); check (b) row count is compared against that city's own typical volume, computed live from its last 5 non-backfill successful runs; check (c) missing expected fields, unchanged. Every check identifies which city and which check failed by name in its notes, both in ingestion_runs and in alert emails. Centralized email-sending (previously duplicated in both ingest scripts) into monitor.js as reportLoudFailure() and reportSilentFailure(). Added migration (dated 2026-08-15): ingestion_runs.is_initial_run, so backfill runs don't skew the typical-volume baseline -- confirmed via query: NYC's 25,081-row and Toronto's 11,577-row backfills correctly flagged true, all other runs false. Updated both ingest-nyc.js and ingest-toronto.js to pass their database client into checkIngestionHealth (needed for the new history-aware checks) and to record is_initial_run on every run. Verified locally (both cities returned city-tagged 'success' notes) and via GitHub Actions manual triggers (both green). Also bumped actions/checkout to v5 and actions/setup-node to v6 in both workflow files, resolving a Node 20 deprecation warning GitHub Actions surfaced -- confirmed these are the current major versions with native Node 24 support, not just a version-number bump.
- **Session 7** -- City-First Search & Filter UI. Rebuilt app/page.js as a Server Component: city (NYC or Toronto) is the first, required choice, shown as two large cards before any filter appears -- reinforces Section 1's city-scoping rule at the UI layer. Once a city is picked, filters (category, area/address, date range) and results live entirely in URL search params, so the page works without client-side JavaScript and every filtered view is a shareable/bookmarkable link. Added lib/db.js -- a shared, cached Postgres pool for the app (separate from scripts/'s pool, reuses the same DATABASE_URL). Known gap surfaced and worked around: Toronto's establishments.area was never populated during Session 5 ingestion (only NYC's borough was mapped in) -- rather than ship a broken area filter for one city, the 'Area or address' field searches both e.area and e.address, so it stays useful for both cities with one control. Each result includes a Google Maps link computed at render time from legal_name + address (no API key, no stored field, per Section 1.3). Verified locally and on the live Vercel deployment: both cities return real, correctly-separated results with working city tags, grade/status badges, and Maps links. One paste-related gotcha discovered and fixed: a JSX opening tag split across lines with just `<a` alone on its own line got corrupted during terminal paste -- fixed by keeping the tag name and first attribute on the same line throughout the file.
- **Session 8** -- Authentication. Chose NextAuth v4 with a Credentials provider (email/password) over magic links -- the schema already had a password_hash column ready for it, and it keeps local testing simple (no dependency on email delivery for every login during development). Built lib/auth.js (NextAuth config), lib/db.js-backed signup at app/api/signup, /login and /signup pages, a protected /dashboard (redirects to /login if unauthenticated), and a sign-out button. Added email verification on top: migration (dated 2026-08-16) adding users.email_verified and a new email_verification_tokens table; signup now generates a token, emails a verify link via Resend (lib/email.js, reusing the same direct-fetch approach as the ingestion scripts' alert emails); /api/verify-email marks the account verified and deletes the token; /api/resend-verification lets a signed-in user request a new link. The dashboard shows a verification banner until confirmed, and the jwt callback re-checks email_verified from the database on every request so the banner clears immediately after clicking the link, without needing to log out and back in. One real bug hit and fixed during testing: the email_verified migration was run in Neon but not verified with a follow-up query, so the column silently didn't exist when a user tried to log in, causing a 500 error and a login that appeared to hang. Fixed by re-running the migration and confirming it landed via information_schema before retrying. Lesson applied going forward: always follow a migration with a confirmation query, not just visual assumption that it worked.
- **Session 8 (addition)** -- Google Sign-In. Added GoogleProvider to lib/auth.js alongside Credentials. Google accounts skip the signup form, so they land with no buyer_category -- built a one-time /complete-profile step (new page + app/api/complete-profile route) that the dashboard redirects to until buyer_category is set, since that field matters for future alert targeting and shouldn't be silently left null. Google-authenticated emails are trusted as pre-verified (email_verified = TRUE on creation, no verification email sent for these accounts). The signIn callback finds-or-creates a users row by email on first Google sign-in; the jwt callback resolves to the same internal integer users.id regardless of which provider was used, so session.user.id stays consistent for both. Set up via Google Cloud Console: OAuth consent screen (External, in Testing publish status) + OAuth Client (Web application) with both localhost and the production Vercel URL registered as authorized origins/redirect URIs. Verified end-to-end locally and on the live Vercel deployment (added GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET to Vercel env vars, redeployed, confirmed working).
- **Session 9** -- Saved Searches (City-Required). Built full CRUD: app/api/saved-searches (GET list, POST create) and app/api/saved-searches/[id] (PATCH edit, DELETE), all scoped to the signed-in user via session.user.id -- no user can read, edit, or delete another user's saved search, enforced with WHERE user_id = $1 on every query, not just at the UI layer. The core rule from Section 1/5 (city_code NOT NULL) is enforced three times: a disabled empty <option> makes the browser itself refuse to submit without a city chosen; a client-side check backs that up in case the DOM is bypassed; and the API route rejects any request without a valid city_code regardless of what the client sends. city_code is intentionally not editable after creation -- wanting a different city means creating a new saved search, matching the blueprint's 'two searches for two cities' model rather than allowing a search to silently change scope. New /saved-searches page (Server Component for auth + initial fetch, client component for the interactive create/delete flow), linked from the dashboard. min_severity is used as a simple 'critical only' toggle for now. Verified locally: submission blocked with no city selected, successful creation with a city, and deletion, all working correctly.
- **Session 10** -- City-Scoped Matching Engine + Isolation Test. Built scripts/lib/matching.js: matchViolation(client, violationId) finds every saved_search whose city_code strictly equals the violation's city_code AND whose category/area/critical-only filters match, then inserts into search_matches (idempotent via the existing UNIQUE constraint). matchUnprocessedViolations() sweeps violations with no search_matches row yet, capped at 500/city per run. Ran the MANDATORY test (scripts/test-cross-city-isolation.js, runnable via node scripts/test-cross-city-isolation.js): created a NYC saved_search and a Toronto violation with deliberately identical category, area text, and severity -- differing ONLY in city -- and proved three things: (1) the matching engine created zero cross-city matches; (2) a same-city control case matched correctly, proving the matcher isn't just silently inert; (3) a raw INSERT bypassing the application matching logic entirely was still blocked by the Session 3 database trigger with the expected 'City mismatch' error. RESULT: 3 passed, 0 failed -- PASS. One real bug hit and fixed during the first test run: the test's own cleanup only deleted search_matches tied to its own test saved_search, but the matcher correctly also matched the test violations against real existing saved_searches, leaving orphaned rows that broke a foreign-key-constrained DELETE. Fixed by cleaning up ALL search_matches referencing the test violation IDs, not just ones from the test's own saved_search -- re-ran clean, 3/3 PASS. Added scripts/run-matching.js as a standalone manual-run entry point; verified against real data (NYC: 500 violations processed, 154 real matches created; TOR: 500 processed, 0 matches). Wiring this into the daily GitHub Actions ingestion workflows so it runs automatically is Session 11's job, not this one.
- **Session 11** -- Customer Email Digest. Built scripts/lib/digest.js implementing the two-path design from Section 6: runCriticalAlerts() sends same-day, one email per user listing all their new CRITICAL-flagged matches; runWeeklyDigest() sends everything else. Both query search_matches WHERE alert_sent_at IS NULL (migration dated 2026-08-17, confirmed applied via information_schema before use) and mark rows sent afterward, so nothing is ever emailed twice across the two paths -- verified directly: after running critical alerts (154 real matches, 1 user notified), the weekly digest correctly reported 0 users/0 matches, since everything was already claimed. Created scripts/run-critical-alerts.js and scripts/run-weekly-digest.js as CLI entry points, and two new GitHub Actions workflows: match-and-alert.yml (daily at 11:00 UTC, after both ingestion jobs -- runs scripts/run-matching.js then run-critical-alerts.js in sequence, finally wiring Session 10's matcher into daily automation as planned) and weekly-digest.yml (Mondays at 12:00 UTC). New GitHub secret EMAIL_FROM added (distinct from the existing ALERT_EMAIL_FROM, which is for owner pipeline-health alerts, not customer-facing emails). Verified end-to-end: local test run delivered a real email to the account's own Resend-verified address; both new workflows manually triggered on GitHub Actions and completed green with no warnings (using the v5/v6 action versions already fixed in Session 6, so this didn't need the Node-deprecation fix other workflows needed).
- **Session 12** -- Admin Dashboard. Scope decision (undefined in blueprint): built ingestion status + median_lag_days trend + a QA queue for violations stuck in category 'other', and deliberately skipped the 'claim queue' mentioned in the Session 12 title -- it's not defined anywhere else in the document and there's no prospects-to-user claiming workflow built yet. Decision logged in the blueprint's own Corrections & Additions Log (Section 10) per the blueprint's own rule for scope changes. New /admin page (admin-gated via users.is_admin, migration dated 2026-08-17) shows: latest run per city (DISTINCT ON), last 20 runs across both cities, a lag trend bar visualization (no charting library -- plain CSS width bars, consistent with the project's minimal-dependency approach), and an interactive QA queue (reclassify or dismiss, via app/api/admin/qa/[id]). Real finding surfaced by the QA queue on first use: every uncategorized violation is Toronto, none NYC, and Toronto's violation_description consistently reads the literal string 'None' -- DineSafe's CSV uses that as a placeholder rather than leaving the field blank. This suggests Toronto's typeDesc field alone rarely contains the pest/sanitation/temperature keywords the Session 4/5 normalizer looks for, so Toronto's category coverage is systematically weaker than NYC's -- worth a dedicated look at Toronto-specific keywords in a future session, not fixed here. One bug hit and fixed before this ever reached Neon: app/api/admin/qa/[id]/route.js's relative import path was one directory level too shallow (../../../../lib/auth instead of ../../../../../lib/auth) due to the extra [id] nesting -- caught by npm run build failing on Vercel with 'Module not found', fixed, confirmed with a clean local npm run build before repushing.
- **Session 13** -- Paddle Integration (built dormant). Opened a fresh Paddle account (distinct email) after the prior account's display-name support ticket left it in a confusing dual-login state -- decision and rationale logged in the blueprint's Corrections & Additions Log. Built entirely in Paddle sandbox, per the blueprint's 'built dormant' instruction -- not linked from the app's nav, reachable only via direct /checkout URL, hardcoded to sandbox mode in code (not env-var controlled) so it can't accidentally go live before Session 14. Migration (dated 2026-08-17): users.paddle_customer_id, paddle_subscription_id, paddle_subscription_status, kept separate from the existing plan (free/pro) column that the app's tier-gating logic will actually check. Built app/checkout/page.js (Paddle.js overlay checkout, passes customData.user_id so the webhook can link a subscription back to our user) and app/api/webhooks/paddle/route.js (verifies the Paddle-Signature header via HMAC-SHA256 over the RAW request body -- read via request.text() before any JSON.parse, since verifying against a re-parsed body is the most common way this kind of check silently breaks -- plus a 30-second replay-attack timestamp check and constant-time comparison). Two real issues hit and fixed during setup, neither a code bug: (1) checkout opened but failed with a generic 'Something went wrong' until the site's domain was explicitly approved in Paddle's checkout settings; (2) even after that, checkout failed with a specific validation error, transaction_default_checkout_url_not_set, until a default checkout/payment link was configured in Paddle's dashboard -- both are one-time account-level settings, not something our code could have caught. Verified end-to-end with a real sandbox test purchase (card 4242 4242 4242 4242): invoice email received from Paddle, and the webhook correctly updated the database -- plan set to 'pro', paddle_customer_id and paddle_subscription_id populated, status 'active' -- confirmed via direct query, not just assumed from the UI.
- **Session 14** -- Tier Gating. Blueprint conflict surfaced and resolved: the blueprint says tier boundaries should be 'finalized using real Session 0 pricing feedback,' but Session 0 outreach was skipped -- decided (with explicit sign-off) to build provisional gating now using the untested $99/month hypothesis, clearly marked as easy to revise later, rather than block on validation that was never going to happen retroactively. Gating design: free plan gets 1 saved search and the weekly digest; Pro gets unlimited saved searches and same-day critical alerts -- chosen because same-day speed on a hot lead is the actual thing worth paying for in this business, not the search feature itself. Enforced in two places: app/api/saved-searches POST checks session.user.plan and a live COUNT before allowing a new search (403 with upgradeRequired flag if at the free limit); scripts/lib/digest.js's runCriticalAlerts query now filters to u.plan = 'pro' -- free users' critical violations aren't lost, they simply stay unclaimed (alert_sent_at IS NULL) and flow into the next weekly digest automatically, since that query has no plan filter -- no extra code needed for that fallback, it falls out of the existing single alert_sent_at design from Session 11. Added plan to lib/auth.js's jwt callback, which already re-reads from the database on every request (the same pattern that made is_admin reliable in Session 12) -- so a Paddle webhook upgrading someone mid-session is reflected immediately, no stale-session gotcha like Session 12 flagged. UI: dashboard shows a plan badge and 'Upgrade to Pro' link for free users; saved-searches page shows live usage ('0 of 1 used') and swaps the create form for an upgrade card once the limit is hit, rather than a bare error message. /checkout is now linked from the app for the first time (previously dormant/unlinked since Session 13) -- still Paddle sandbox only; going live with real payments remains a separate, deliberate future step, not done here. Verified locally against both real accounts: the Session 13 test-purchase account (plan='pro') has no limit and a green badge; the free account correctly blocks at 1 saved search and shows the upgrade prompt.
- **Addendum (2026-08-21)** -- Search Usage Tracking. Prompted by a direct question: since NYC/Toronto violation data has an explicit open/no-restrictions license and is freely available from the government source directly, and since Session 15's outbound-email plan depends on prospects being able to verify real value before ever being asked to pay, a full paywall on live search was considered and deliberately rejected in favor of keeping search free -- consistent with Session 7's original 'free top-of-funnel view' design and Session 14's own established philosophy that same-day alert speed, not raw search access, is the actual paid differentiator. Decision: keep search free for now, but make the eventual paywall-or-not decision data-driven rather than a guess. Added migration: search_events (city_code, category_filter, area_filter, result_count, created_at) -- anonymous, no user linkage, no IP address, fire-and-forget (wrapped in try/catch so a logging failure can never break the public search page). app/page.js's getResults() now logs one row per real search. Admin dashboard (Session 12) extended with a 'Public search usage' panel: 7-day total by city, plus a 14-day daily bar chart, using the same lightweight CSS-bar-chart approach as the existing lag trend -- no new dependency. Verified locally: real searches across both cities showed up correctly in the admin panel within the same session.

## Session 15 (continued) -- 2026-08-25 -- Target list build-out, contact discovery, and a costly billing lesson

### What got built
- Step 1: Real ingestion from NY DEC "Currently Registered Pesticide Businesses and Agencies" (resource h8u2-6ejg), filtered to NY state + categories 7A/7C/7F (Structural & Rodent, Termite, Food Processing), then to NYC zip codes client-side. Result: 1,344 pest control prospects -- more than double the ~572 estimate in the original handoff, because that estimate was almost certainly based on a category-7A-only pull. 7A alone would have MISSED real, confirmed businesses (Empire Exterminating, Horizon Pest Management, Onsite Pest Solutions), since they're registered under 7C or 7F instead. 7B (Fumigation) was deliberately excluded -- its actual DEC category description covers lab rodent elimination, burrow fumigation, and transport/storage fumigation, which reads as specialized industrial work, not general restaurant-adjacent pest control.

- Contact discovery went through three methods, in order:
  1. OSM Overpass + literal domain-guessing (the blueprint's original Session 15 Steps 2-3) -- real hit rate on a 10-business validation sample: 20%, below the blueprint's own 30-40% gate. Correctly stopped per the gate rule instead of proceeding.
  2. Free search API (Tavily) + domain-token verification -- also validated on the same 10 businesses, went through three script iterations to fix false positives (generic short tokens like "EX" matching inside unrelated words; a business's own DBA brand name not overlapping its legal name at all -- "Annihilator eX-Termination, Inc." legitimately operates as "You Got Bugs").
  3. Google Places API (New) Text Search -- used for the real 1,344-row enrichment. Result: 999-1002 candidates found (varies slightly run to run), later corrected by a domain-substring scoring pass to 385 contact_verified = true (~29%), consistent with every other method tried this session. That consistency across three unrelated techniques is itself evidence the ~30% ceiling is real, not a tooling artifact.

- Cleaning companies (271 total, 269 with phone, 235 with website): no DEC-style registry exists for commercial cleaning, so this used Places API category search directly across 9 NYC neighborhoods instead of name-matching against a known list. Deduped by Places' own place ID. contact_verified is false on all of these by design -- there's no source name to score them against the way pest control had, not a gap that needs fixing.

### IMPORTANT -- costly mistake, read before touching Places API again
Every phone/website enrichment script this session (pest control AND cleaning) requested places.nationalPhoneNumber and places.websiteUri in the FieldMask. Both of these fields trigger Google's Enterprise SKU (1,000 free calls/month), not the Pro SKU (5,000 free/month) that plain Text Search normally bills at. This was incorrectly believed to be Pro-tier throughout most of the session, based on a general free-tier-ceiling check that was never cross-referenced against the actual field-to-SKU mapping in Google's own docs. Real usage this session (~4,300+ calls, most requesting phone+website) blew past the real 1,000/month Enterprise ceiling, resulting in an unplanned charge (~2,000 TWD). A Google Cloud billing support case was opened requesting goodwill credit; outcome unknown as of this entry.

Going forward: if you need phone/website from Places again, budget for Enterprise tier's real 1,000/month ceiling, not 5,000. Better default: use Places for displayName/formattedAddress only (Pro tier, genuinely free) to confirm a business exists, and get phone/email via the free own-site scraping approach or Tavily search instead. Tavily is a separate service with its own free 1,000 credits/month, unrelated to Google's billing.

### Known bugs fixed this session (for context if similar patterns recur)
- Domain-verification scoring initially used exact-word-token matching against a concatenated DOMAIN string (no internal word separators), which almost never matches even for correct pairs. Fixed by switching to substring matching with a minimum token length of 4.
- A liveness check using HTTP HEAD wrongly rejected known-good, working sites. Fixed by switching to GET.
- The contact_method CHECK constraint didn't get updated before a script tried to write 'tavily_search' as a value -- caused failures on ~2 rows before the constraint was expanded mid-session.

### Current real numbers (verified via direct query, 2026-08-25)
- pest_control: 1,344 total, 385 contact_verified, 671 with phone, 526 with website.
- cleaning: 271 total, 269 with phone, 235 with website, contact_verified intentionally false on all (never scored).

### Still open
- ~959 pest control prospects have no verified contact yet. A Tavily-based discovery pass was started but interrupted by a per-request rate limit (not the monthly credit cap -- only ~367/1,000 monthly credits were used). This pool is disproportionately the businesses NEITHER Places nor earlier layers could verify -- expect a real hit rate lower than ~30% if resumed.
- No email data collected yet for either category at scale.